import { Component } from 'vue'
import { mapGetters, mapActions } from 'vuex'

export default {
  name: 'Info',
  computed: {
    ...mapGetters({
    })
  },

  created(this: Component) {
  },

  watch: {
  },

  methods: {
    ...mapActions({
    })
  },

  components: {
  }
}
